# RAG Comparison Backend
## Setup
- make sure to insall all python dependencies listed int `requirements.txt`
- configure the application using `config.yml`
- create a `.env` file in backend root folder containing Mistral API keys